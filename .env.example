# FraudShield AI Environment Configuration
# Required for autonomous investigation summaries and generative fraud reports:
GEMINI_API_KEY=

# Port configuration (defaults to 3000 in dev/Cloud Run, auto-assigned by Render in production):
PORT=3000

# Node environment (production / development)
NODE_ENV=production
